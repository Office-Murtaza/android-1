<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:background="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:titleTextColor="@color/colorText" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/contentContainerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/toolbarView" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/errorView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawablePadding="10dp"
            android:paddingBottom="10dp"
            android:visibility="gone"
            app:layout_constraintBottom_toTopOf="@id/retryButtonView"
            app:layout_constraintEnd_toEndOf="@id/contentContainerView"
            app:layout_constraintStart_toStartOf="@id/contentContainerView"
            app:layout_constraintTop_toTopOf="@id/contentContainerView"
            app:layout_constraintVertical_chainStyle="packed"
            tools:drawableTop="@drawable/ic_internet_unavailable"
            tools:text="@string/error_internet_unavailable"
            tools:visibility="visible" />

        <Button
            android:id="@+id/retryButtonView"
            style="@style/Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Retry"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/contentContainerView"
            app:layout_constraintEnd_toEndOf="@id/contentContainerView"
            app:layout_constraintStart_toStartOf="@id/contentContainerView"
            app:layout_constraintTop_toBottomOf="@id/errorView"
            tools:visibility="visible" />

        <ProgressBar
            android:id="@+id/progressView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/contentContainerView"
            app:layout_constraintEnd_toEndOf="@id/contentContainerView"
            app:layout_constraintStart_toStartOf="@id/contentContainerView"
            app:layout_constraintTop_toTopOf="@id/contentContainerView" />
    </LinearLayout>

</LinearLayout>