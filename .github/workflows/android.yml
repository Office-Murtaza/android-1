name: Android CI

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - 'android/**'

jobs:
  build:
    name: Build APKs
    runs-on: ubuntu-latest
    env:
      working-directory: ./android

    steps:
      - uses: actions/checkout@v2
      - name: set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Build Debug
        run: bash ./gradlew assembleDebug 
        working-directory: ${{ env.working-directory }}
        
      - name: Distribute to Firebase
        run: bash export FIREBASE_TOKEN=1//0c6viHtVMDtEoCgYIARAAGAwSNwF-L9IrR1xmTnIQONgpfI9xiLAOV61mxhIG3YMGZtVuv_nsBcEH1CQrjNDK2Z-KZpHX1Yh5OJM
             | bash ./gradlew --stop
             | bash ./gradlew assembleDebug appDistributionUploadDevDebug
